enum AllThemes {
  light,
  dark,
}

enum UserRole {
  poc,
  new_learner,
}

///New Learner
///POC
enum ToastType {
  success,
  error,
  warning,
}

enum LevelOfAccessColor {
  Green,
  Blue,
  White,
}

enum StepperStatus {
  locked,
  in_progress,
  completed,
}

enum BookingStatus {
  pendingApproval,
  bookingAccepted,
  bookingRejected,
  visitCompleted,
}
